spring:
    application:
        name: @project.artifactId@@env@
    cloud:
        consul:
            host: consul.iishoni.com
            discovery:
                instance-id: ${spring.application.name}
                prefer-ip-address: true
                ip-address: 120.78.150.87

server:
    port: 10000
